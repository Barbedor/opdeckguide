---
import MainLayout from "../../layouts/MainLayout.astro";

const title = "Monkey.D.Luffy ST29 Deck Guide";
const description =
	"Monkey.D.Luffy ST29 deck guide for One Piece Card Game with decklist, curve reference, mulligan targets, options, and matchup focus leaders.";

const icon = "/assets/guides/monkey-d-luffy-st29.png";

const decklist = [
	{ code: "ST29-001", name: "Monkey.D.Luffy", count: 1, img: "/Cards/ST29/ST29-001.jpg", role: "Leader" },
	{ code: "OP07-099", name: "Usopp", count: 4, img: "/Cards/OP07/OP07-099.png", role: "Character" },
	{ code: "OP13-113", name: "Lilith", count: 4, img: "/Cards/OP13/OP13-113.jpg", role: "Character" },
	{ code: "ST29-009", name: "Nico Robin", count: 4, img: "/Cards/ST29/ST29-009.jpg", role: "Character" },
	{ code: "ST29-004", name: "Sanji", count: 4, img: "/Cards/ST29/ST29-004.jpg", role: "Character" },
	{ code: "OP06-104", name: "Kikunojo", count: 4, img: "/Cards/OP06/OP06-104.png", role: "Character" },
	{ code: "OP08-106", name: "Nami", count: 4, img: "/Cards/OP08/OP08-106.png", role: "Character" },
	{ code: "ST29-005", name: "Jinbe", count: 4, img: "/Cards/ST29/ST29-005.jpg", role: "Character" },
	{ code: "OP09-107", name: "Nico Robin", count: 2, img: "/Cards/OP09/OP09-107.jpg", role: "Character" },
	{ code: "OP12-119", name: "Bartholomew Kuma", count: 4, img: "/Cards/OP12/OP12-119.jpg", role: "Character" },
	{ code: "OP10-112", name: "Eustass \"Captain\" Kidd", count: 4, img: "/Cards/OP10/OP10-112.jpg", role: "Character" },
	{ code: "OP13-108", name: "Jewelry Bonney", count: 4, img: "/Cards/OP13/OP13-108.jpg", role: "Character" },
	{ code: "OP06-115", name: "You're the One Who Should Disappear", count: 1, img: "/Cards/OP06/OP06-115.png", role: "Event" },
	{ code: "ST29-016", name: "Kizaru!! Compared to Two Years Ago We're a Hundred Times Stronger Now!!", count: 3, img: "/Cards/ST29/ST29-016.jpg", role: "Event" },
	{ code: "ST13-017", name: "Flame Dragon King", count: 4, img: "/Cards/ST13/ST13-017.png", role: "Event" },
];

const optionsGroups = [
	{
		cards: [
			{ code: "OP14-100", name: "Absalom", img: "/Cards/OP14/OP14-100.jpg" },
			{ code: "OP14-110", name: "Dr. Hogback", img: "/Cards/OP14/OP14-110.jpg" },
			{ code: "OP14-111", name: "Perona", img: "/Cards/OP14/OP14-111.jpg" },
			{ code: "OP14-104", name: "Gecko Moria", img: "/Cards/OP14/OP14-104.jpg" },
		],
		note:
			"Thriller Pack is a good option up to EB03, have some powerful Trigger and help if you need starve for more time.",
	},
	{
		cards: [
			{ code: "OP07-116", name: "Blaze Slice", img: "/Cards/OP07/OP07-116.png" },
			{ code: "OP14-118", name: "You’ll Frighten Me …", img: "/Cards/OP14/OP14-118.jpg" },
		],
		note:
			"If you don’t have Flame Dragon King or need cheapest option you can play Blaze Slice and You’ll Frighten Me …, they are a good alternative but less effective for the line that has the deck.",
	},
];

const mulliganCards = [
	{ code: "ST13-017", name: "Flame Dragon King", img: "/Cards/ST13/ST13-017.png" },
	{ code: "OP13-113", name: "Lilith", img: "/Cards/OP13/OP13-113.jpg" },
	{ code: "OP06-104", name: "Kikunojo", img: "/Cards/OP06/OP06-104.png" },
	{ code: "ST29-004", name: "Sanji", img: "/Cards/ST29/ST29-004.jpg" },
];

const curveFirst = [
	{
		label: "1 DON",
		notes: "Search 2C spell or missing Character.",
		cards: [{ code: "OP13-113", name: "Lilith", img: "/Cards/OP13/OP13-113.jpg" }],
	},
	{
		label: "3 DON",
		notes: "Fix your lives and swing 6K vs leader 6K base power or use another searcher vs 5K leader.",
		cards: [{ code: "ST13-017", name: "Flame Dragon King", img: "/Cards/ST13/ST13-017.png" }],
	},
	{
		label: "5 DON",
		notes: "Depends on the situation but this is your only removal on curve, but Sanji/Kiku are anyway a good options for value.",
		cards: [{ code: "OP08-106", name: "Nami", img: "/Cards/OP08/OP08-106.png" }],
	},
	{
		label: "7 DON",
		notes: "This is only turn you can’t play on curve up to EB03 so Robin for burn opponent lives or any 4C for growing your board.",
		cards: [{ code: "OP09-107", name: "Nico Robin", img: "/Cards/OP09/OP09-107.jpg" }],
	},
	{
		label: "9 DON",
		notes: "Burn! Burn! Burn… and rush!",
		cards: [{ code: "OP13-108", name: "Jewelry Bonney", img: "/Cards/OP13/OP13-108.jpg" }],
	},
	{
		label: "Late game",
		notes: "IMO this is your best finisher. If you correctly setup that 1 or alternatively play boss monster and starve.",
		cards: [{ code: "ST29-016", name: "Kizaru!! Compared to Two Years Ago We're a Hundred Times Stronger Now!!", img: "/Cards/ST29/ST29-016.jpg" }],
	},
];

const curveSecond = [
	{
		label: "2 DON",
		notes: "Stay 2 DON open for cast the spell and fix your lives.",
		cards: [{ code: "ST13-017", name: "Flame Dragon King", img: "/Cards/ST13/ST13-017.png" }],
	},
	{
		label: "4 DON",
		notes: "Play Sanji if you need Robin on curve or 2K or play Kiku to make more pressure vs 6K base power leader.",
		cards: [
			{ code: "OP06-104", name: "Kikunojo", img: "/Cards/OP06/OP06-104.png" },
			{ code: "ST29-004", name: "Sanji", img: "/Cards/ST29/ST29-004.jpg" },
		],
	},
	{
		label: "6 DON",
		notes: "If your opponent has thought about defending himself too much, make him pay or alternatively play Kuma for a few more Triggers in the lives.",
		cards: [
			{ code: "OP09-107", name: "Nico Robin", img: "/Cards/OP09/OP09-107.jpg" },
			{ code: "OP12-119", name: "Bartholomew Kuma", img: "/Cards/OP12/OP12-119.jpg" },
		],
	},
	{
		label: "8 DON",
		notes: "Kidd fit perfectly in this deck, is a big body and for his effect you can fix your hand from white cards to save him.",
		cards: [{ code: "OP10-112", name: "Eustass \"Captain\" Kidd", img: "/Cards/OP10/OP10-112.jpg" }],
	},
	{
		label: "10 DON",
		notes: "Burn! Burn! Burn... and rush!",
		cards: [{ code: "OP13-108", name: "Jewelry Bonney", img: "/Cards/OP13/OP13-108.jpg" }],
	},
	{
		label: "Late game",
		notes: "IMO this is your best finisher. If you correctly setup that 1 or alternatively play boss monster and starve.",
		cards: [{ code: "ST29-016", name: "Kizaru!! Compared to Two Years Ago We're a Hundred Times Stronger Now!!", img: "/Cards/ST29/ST29-016.jpg" }],
	},
];

const tips = [
	{
		title:
			"Just as a reminder, remember that the spell does not give an additional +1K and you can only close with a 14K Leader so the best option is to set up your board first.",
		cards: [{ code: "ST29-016", name: "Kizaru!! Compared to Two Years Ago We're a Hundred Times Stronger Now!!", img: "/Cards/ST29/ST29-016.jpg" }],
	},
	{
		title:
			"This spell is really strong in early game to fix the lives but remember to order them correctly and if you see another one in the lives I recommend putting it for penultimate so that you can modify the last one again… maybe with a 0C spell.",
		cards: [{ code: "ST13-017", name: "Flame Dragon King", img: "/Cards/ST13/ST13-017.png" }],
	},
];

const matchups = [
	{
		label: "Positive MU",
		icon: "↑",
		leader: { code: "OP14-041", name: "Boa Hancock", img: "/Cards/OP14/OP14-041.jpg" },
		notes: ["Start 2nd", "Manage your lifes", "You have more Trigger"],
	},
	{
		label: "Slight Adv.",
		icon: "↑",
		leader: { code: "OP14-060", name: "Donquixote Doflamingo", img: "/Cards/OP14/OP14-060.jpg" },
		notes: ["Start 2nd", "Manage your board"],
	},
	{
		label: "Even",
		icon: "≈",
		leader: { code: "OP13-079", name: "Imu", img: "/Cards/OP13/OP13-079.jpg" },
		notes: ["Start 2nd", "Manage your board", "Dice dependent"],
	},
	{
		label: "Slight Disadv.",
		icon: "↓",
		leader: { code: "OP11-040", name: "Monkey.D.Luffy", img: "/Cards/OP11/OP11-040.jpg" },
		notes: ["Start 2nd", "Burn his lifes", "Roger is a prob"],
	},
	{
		label: "Negative MU",
		icon: "↓",
		leader: { code: "OP13-002", name: "Ace", img: "/Cards/OP13/OP13-002.jpg" },
		notes: ["Start 2nd", "Manage your board", "6K is a prob"],
	},
	{
		label: "Positive MU",
		icon: "↑",
		leader: { code: "OP14-020", name: "Dracule Mihawk", img: "/Cards/OP14/OP14-020.jpg" },
		notes: ["Start 2nd", "Go aggro and burn his lifes"],
	},
];
---

<MainLayout title={title} description={description}>
	<section class="hero guide-hero">
		<div class="container hero-grid">
			<div>
				<span class="hero-badge">March Guide</span>
				<h1>Monkey.D.Luffy ST29 Deck Guide</h1>
				<p>
					Complete reference guide for Monkey.D.Luffy ST29: decklist, curve checkpoints, mulligan priorities,
					tech options, and matchup focus leaders. Built for fast reading between rounds.
				</p>
				<div class="hero-actions">
					<a class="btn btn-primary" href="#decklist">Jump to Decklist</a>
					<a class="btn btn-secondary" href="#matchups">Matchups</a>
				</div>
			</div>
			<div class="guide-icon">
				<img src={icon} alt="Monkey.D.Luffy ST29 guide icon" loading="lazy" />
			</div>
		</div>
	</section>

	<section id="decklist" class="list-section">
		<div class="container">
			<div class="section-header">
				<div>
					<h2>Decklist</h2>
					<p>Main list for Monkey.D.Luffy ST29.</p>
				</div>
			</div>
			<div class="deck-grid">
				{decklist.map((card) => (
					<button
						class="deck-card"
						type="button"
						data-card-preview
						data-full={card.img}
						data-name={`${card.code} ${card.name}`}
					>
						<img loading="lazy" src={card.img} alt={`${card.code} ${card.name}`} />
						<div class="deck-info">
							<span class="deck-count">{card.count}x</span>
							<div>
								<div class="deck-name">{card.name}</div>
								<div class="deck-code">{card.code}</div>
							</div>
						</div>
						<span class="deck-role">{card.role}</span>
					</button>
				))}
			</div>
		</div>
	</section>

	<section id="options" class="list-section">
		<div class="container">
			<div class="section-header">
				<div>
					<h2>Options & Tech Cards</h2>
					<p>Alternative cards and tech options.</p>
				</div>
			</div>
			<div class="options-stack">
				{optionsGroups.map((group) => (
					<div class="option-group">
						<div class="options-grid">
							{group.cards.map((card) => (
								<button
									class="option-card"
									type="button"
									data-card-preview
									data-full={card.img}
									data-name={`${card.code} ${card.name}`}
								>
									<img loading="lazy" src={card.img} alt={`${card.code} ${card.name}`} />
									<div class="option-info">
										<div class="deck-name">{card.name}</div>
										<div class="deck-code">{card.code}</div>
									</div>
								</button>
							))}
						</div>
						<p class="section-note">{group.note}</p>
					</div>
				))}
			</div>
		</div>
	</section>

	<section id="mulligan" class="list-section">
		<div class="container">
			<div class="section-header">
				<div>
					<h2>Mulligan</h2>
					<p>2C spell is your best start in any curve for fix lives. Lilith if you go 1st or Kiku/Sanji for the 2nd curve.</p>
				</div>
			</div>
			<div class="mulligan-grid">
				{mulliganCards.map((card) => (
					<button
						class="mini-card"
						type="button"
						data-card-preview
						data-full={card.img}
						data-name={`${card.code} ${card.name}`}
					>
						<img loading="lazy" src={card.img} alt={`${card.code} ${card.name}`} />
						<div class="mini-label">{card.name}</div>
					</button>
				))}
			</div>
			<p class="mulligan-text">If you don't see this combo mulligan is the way.</p>
		</div>
	</section>

	<section id="curve" class="list-section">
		<div class="container">
			<div class="section-header">
				<div>
					<h2>Curve Guide</h2>
					<p>Turn-by-turn curve reminders for going first or second.</p>
				</div>
			</div>
			<div class="curve-block">
				<h3>Curve 1st</h3>
				<div class="curve-rows">
					{curveFirst.map((slot) => (
						<div class="curve-row">
							<div class="curve-label">{slot.label}</div>
							<div class="curve-cards">
								{slot.cards.map((card) => (
									<button
										class="mini-card"
										type="button"
										data-card-preview
										data-full={card.img}
										data-name={`${card.code} ${card.name}`}
									>
										<img loading="lazy" src={card.img} alt={`${card.code} ${card.name}`} />
									</button>
								))}
							</div>
							<div class="curve-note">{slot.notes}</div>
						</div>
					))}
				</div>
			</div>
			<div class="curve-block">
				<h3>Curve 2nd</h3>
				<div class="curve-rows">
					{curveSecond.map((slot) => (
						<div class="curve-row">
							<div class="curve-label">{slot.label}</div>
							<div class="curve-cards">
								{slot.cards.map((card) => (
									<button
										class="mini-card"
										type="button"
										data-card-preview
										data-full={card.img}
										data-name={`${card.code} ${card.name}`}
									>
										<img loading="lazy" src={card.img} alt={`${card.code} ${card.name}`} />
									</button>
								))}
							</div>
							<div class="curve-note">{slot.notes}</div>
						</div>
					))}
				</div>
			</div>
		</div>
	</section>

	<section id="tips" class="list-section">
		<div class="container">
			<div class="section-header">
				<div>
					<h2>Tips</h2>
					<p>Key cards highlighted in the guide.</p>
				</div>
			</div>
			<div class="tips-grid">
				{tips.map((tip) => (
					<div class="tip-card">
						<div class="tip-cards">
							{tip.cards.map((card) => (
								<button
									class="mini-card"
									type="button"
									data-card-preview
									data-full={card.img}
									data-name={`${card.code} ${card.name}`}
								>
									<img loading="lazy" src={card.img} alt={`${card.code} ${card.name}`} />
								</button>
							))}
						</div>
						<p>{tip.title}</p>
					</div>
				))}
			</div>
		</div>
	</section>

	<section id="matchups" class="list-section">
		<div class="container">
			<div class="section-header">
				<div>
					<h2>Matchups</h2>
					<p>Key leaders for the matchup spread.</p>
				</div>
			</div>
			<div class="matchups-grid">
				{matchups.map((matchup) => (
					<div class="matchup-card">
						<div class={`matchup-badge ${matchup.label === "Negative MU" ? "badge-down" : matchup.label === "Slight Disadv." ? "badge-down" : matchup.label === "Even" ? "badge-even" : "badge-up"}`}>
							<span class="badge-icon">{matchup.icon}</span>
							<span>{matchup.label}</span>
						</div>
						<button
							class="mini-card"
							type="button"
							data-card-preview
							data-full={matchup.leader.img}
							data-name={`${matchup.leader.code} ${matchup.leader.name}`}
						>
							<img loading="lazy" src={matchup.leader.img} alt={`${matchup.leader.code} ${matchup.leader.name}`} />
						</button>
						<div class="matchup-name">{matchup.leader.name}</div>
						<ul>
							{matchup.notes.map((note) => (
								<li>{note}</li>
							))}
						</ul>
					</div>
				))}
			</div>
		</div>
	</section>

	<div class="card-modal" data-modal aria-hidden="true">
		<div class="card-modal-inner" role="dialog" aria-modal="true" aria-label="Card preview">
			<button class="modal-close" type="button" data-close>Close</button>
			<img src="" alt="" data-modal-image />
		</div>
	</div>
</MainLayout>

<script>
	const previewButtons = document.querySelectorAll("[data-card-preview]");
	const modal = document.querySelector("[data-modal]");
	const modalImage = document.querySelector("[data-modal-image]");
	const closeButton = document.querySelector("[data-close]");

	const openModal = (button) => {
		if (!modal || !modalImage) return;
		const full = button.getAttribute("data-full");
		const name = button.getAttribute("data-name") || "Card preview";
		modalImage.setAttribute("src", full);
		modalImage.setAttribute("alt", name);
		modal.classList.add("is-open");
		modal.setAttribute("aria-hidden", "false");
	};

	const closeModal = () => {
		if (!modal) return;
		modal.classList.remove("is-open");
		modal.setAttribute("aria-hidden", "true");
	};

	previewButtons.forEach((button) => {
		button.addEventListener("click", () => openModal(button));
	});

	closeButton?.addEventListener("click", closeModal);
	modal?.addEventListener("click", (event) => {
		if (event.target === modal) closeModal();
	});
	window.addEventListener("keydown", (event) => {
		if (event.key === "Escape") closeModal();
	});
</script>

<style>
	.guide-hero .hero-grid {
		grid-template-columns: minmax(0, 1.1fr) minmax(0, 0.9fr);
	}

	.guide-icon {
		display: grid;
		place-items: center;
	}

	.guide-icon img {
		width: min(320px, 90%);
		border-radius: 18px;
		border: 1px solid rgba(255, 255, 255, 0.08);
		box-shadow: var(--shadow);
	}

	.deck-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
		gap: 1.2rem;
	}

	.deck-card {
		background: rgba(12, 17, 30, 0.9);
		border: 1px solid var(--border);
		border-radius: var(--radius-md);
		padding: 0.9rem;
		display: grid;
		gap: 0.75rem;
		text-align: left;
		cursor: pointer;
		transition: transform 0.2s ease, border 0.2s ease, box-shadow 0.2s ease;
	}

	.deck-card:hover,
	.deck-card:focus-visible {
		transform: translateY(-4px);
		border-color: rgba(255, 107, 61, 0.4);
		box-shadow: var(--shadow);
		outline: none;
	}

	.deck-card img {
		width: 100%;
		max-width: 150px;
		margin: 0 auto;
		border-radius: 12px;
	}

	.deck-info {
		display: grid;
		grid-template-columns: auto 1fr;
		gap: 0.6rem;
		align-items: center;
	}

	.deck-count {
		background: rgba(255, 107, 61, 0.15);
		color: var(--accent);
		font-weight: 700;
		border-radius: 999px;
		padding: 0.3rem 0.7rem;
		font-size: 0.9rem;
	}

	.deck-name {
		font-weight: 600;
	}

	.deck-code {
		color: var(--text-300);
		font-size: 0.85rem;
	}

	.deck-role {
		color: var(--text-300);
		font-size: 0.85rem;
		text-transform: uppercase;
		letter-spacing: 1px;
	}

	.options-grid {
		display: flex;
		flex-wrap: wrap;
		justify-content: center;
		align-items: flex-start;
		gap: 1rem;
	}

	.options-stack {
		display: grid;
		gap: 1.2rem;
	}

	.option-group {
		display: grid;
		gap: 1rem;
		padding: 1rem;
		border: 1px solid var(--border);
		border-radius: var(--radius-md);
		background: rgba(12, 17, 30, 0.55);
	}

	.option-card {
		background: rgba(12, 17, 30, 0.9);
		border: 1px solid var(--border);
		border-radius: var(--radius-md);
		padding: 0.8rem;
		display: grid;
		gap: 0.6rem;
		width: min(220px, 100%);
		justify-items: center;
		text-align: center;
		cursor: pointer;
		transition: transform 0.2s ease, border 0.2s ease, box-shadow 0.2s ease;
	}

	.option-card:hover,
	.option-card:focus-visible {
		transform: translateY(-4px);
		border-color: rgba(255, 107, 61, 0.4);
		box-shadow: var(--shadow);
		outline: none;
	}

	.option-card img {
		width: 100%;
		max-width: 220px;
		margin: 0 auto;
		border-radius: 10px;
	}

	.option-info {
		display: grid;
		gap: 0.2rem;
		text-align: center;
	}

	.option-note {
		margin: 0.4rem 0 0;
		color: var(--text-300);
		font-size: 0.9rem;
	}

	.note-block {
		background: rgba(14, 18, 30, 0.7);
		border: 1px solid var(--border);
		border-radius: var(--radius-md);
		padding: 1.2rem 1.4rem;
	}

	.mulligan-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
		gap: 1rem;
		margin-bottom: 1rem;
	}

	.mulligan-text {
		color: var(--text-200);
	}

	.curve-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
		gap: 1.2rem;
	}

	.curve-block {
		background: rgba(12, 17, 30, 0.9);
		border: 1px solid var(--border);
		border-radius: var(--radius-md);
		padding: 1.2rem;
		margin-bottom: 1.2rem;
	}

	.curve-rows {
		display: grid;
		gap: 1rem;
		margin-top: 0.8rem;
	}

	.curve-row {
		display: grid;
		grid-template-columns: 80px minmax(0, 1fr) minmax(0, 1.2fr);
		gap: 1rem;
		align-items: center;
	}

	.curve-label {
		font-weight: 700;
		color: var(--accent);
	}

	.curve-cards {
		display: flex;
		flex-wrap: wrap;
		gap: 0.6rem;
	}

	.curve-note {
		color: var(--text-300);
	}

	.mini-card {
		border: none;
		background: transparent;
		cursor: pointer;
		padding: 0;
		border-radius: 10px;
	}

	.mini-card img {
		width: 120px;
		border-radius: 10px;
		display: block;
		box-shadow: 0 10px 20px rgba(0, 0, 0, 0.35);
	}

	.mini-label {
		margin-top: 0.4rem;
		font-size: 0.85rem;
		color: var(--text-300);
		text-align: center;
	}

	.tips-grid {
		display: grid;
		gap: 1.2rem;
	}

	.tip-card {
		background: rgba(12, 17, 30, 0.9);
		border: 1px solid var(--border);
		border-radius: var(--radius-md);
		padding: 1rem 1.2rem;
		display: grid;
		gap: 0.8rem;
	}

	.tip-cards {
		display: flex;
		gap: 0.8rem;
		flex-wrap: wrap;
	}

	.matchups-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
		gap: 1rem;
	}

	.matchup-card {
		background: rgba(12, 17, 30, 0.9);
		border: 1px solid var(--border);
		border-radius: var(--radius-md);
		padding: 1rem;
		display: grid;
		gap: 0.6rem;
	}

	.matchup-badge {
		font-size: 0.8rem;
		text-transform: uppercase;
		letter-spacing: 1px;
		font-weight: 700;
		display: inline-flex;
		align-items: center;
		gap: 0.45rem;
		padding: 0.25rem 0.6rem;
		border-radius: 999px;
		border: 1px solid rgba(255, 255, 255, 0.08);
		background: rgba(12, 17, 30, 0.8);
	}

	.badge-icon {
		width: 26px;
		height: 26px;
		border-radius: 999px;
		display: grid;
		place-items: center;
		font-size: 1.1rem;
		font-weight: 800;
		color: #0b0f1a;
	}

	.badge-up .badge-icon {
		background: linear-gradient(135deg, #22c55e, #86efac);
	}

	.badge-down .badge-icon {
		background: linear-gradient(135deg, #ef4444, #fca5a5);
	}

	.badge-even .badge-icon {
		background: linear-gradient(135deg, #f59e0b, #fbbf24);
	}

	.matchup-name {
		font-weight: 600;
	}

	.curve-card {
		background: rgba(12, 17, 30, 0.9);
		border: 1px solid var(--border);
		border-radius: var(--radius-md);
		padding: 1.2rem;
	}

	.section-note {
		color: var(--text-300);
		max-width: 68ch;
		margin: 0 auto;
		text-align: center;
		line-height: 1.65;
		font-size: clamp(0.95rem, 0.88rem + 0.18vw, 1rem);
	}

	.card-modal {
		position: fixed;
		inset: 0;
		background: rgba(6, 8, 14, 0.85);
		display: none;
		align-items: center;
		justify-content: center;
		padding: 2rem;
		z-index: 40;
	}

	.card-modal.is-open {
		display: flex;
	}

	.card-modal-inner {
		max-width: min(520px, 90vw);
		max-height: 90vh;
		background: rgba(10, 14, 24, 0.9);
		border: 1px solid var(--border);
		border-radius: var(--radius-lg);
		padding: 1rem 1rem 1.5rem;
		position: relative;
		box-shadow: var(--shadow);
	}

	.card-modal-inner img {
		width: 100%;
		max-height: 80vh;
		object-fit: contain;
	}

	.modal-close {
		position: absolute;
		top: 0.75rem;
		right: 0.75rem;
		background: rgba(255, 255, 255, 0.05);
		border: 1px solid var(--border);
		color: var(--text-100);
		border-radius: 999px;
		padding: 0.35rem 0.8rem;
		cursor: pointer;
		font-size: 0.85rem;
	}

	@media (max-width: 900px) {
		.guide-hero .hero-grid {
			grid-template-columns: 1fr;
		}
	}

	@media (max-width: 640px) {
		.option-group {
			padding: 0.85rem;
		}

		.deck-card img {
			max-width: 120px;
		}

		.option-card img {
			max-width: 170px;
		}

		.mini-card img {
			width: 90px;
		}
	}
	@media (max-width: 640px) {
		.deck-grid {
			grid-template-columns: repeat(3, minmax(0, 1fr));
			gap: 0.35rem;
		}

		.deck-card {
			padding: 0.35rem;
			gap: 0.4rem;
			border-radius: 12px;
			text-align: center;
		}

		.deck-card img {
			max-width: 94px;
			border-radius: 8px;
		}

		.deck-info {
			grid-template-columns: 1fr;
			justify-items: center;
			gap: 0.28rem;
		}

		.deck-count {
			padding: 0.18rem 0.45rem;
			font-size: 0.68rem;
			line-height: 1;
		}

		.deck-name {
			font-size: 0.68rem;
			line-height: 1.15;
			display: -webkit-box;
			-webkit-line-clamp: 2;
			-webkit-box-orient: vertical;
			overflow: hidden;
		}

		.deck-code {
			font-size: 0.66rem;
			line-height: 1.1;
		}

		.deck-role {
			display: none;
		}
	}
</style>

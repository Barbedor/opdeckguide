---
import MainLayout from "../../layouts/MainLayout.astro";

const title = "Dracule Mihawk OP14 Deck Guide";
const description =
	"Dracule Mihawk OP14 deck guide for One Piece Card Game with decklist, curve reference, mulligan targets, options, and matchup focus leaders.";

const icon = "/assets/guides/dracule-mihawk-op14.png";

const decklist = [
	{ code: "OP14-020", name: "Dracule Mihawk", count: 1, img: "/Cards/OP14/OP14-020.jpg", role: "Leader" },
	{ code: "OP12-034", name: "Perona", count: 4, img: "/Cards/OP12/OP12-034.jpg", role: "Character" },
	{ code: "OP14-023", name: "Kikunojo", count: 4, img: "/Cards/OP14/OP14-023.jpg", role: "Character" },
	{ code: "OP10-032", name: "Tashigi", count: 4, img: "/Cards/OP10/OP10-032.jpg", role: "Character" },
	{ code: "OP08-023", name: "Carrot", count: 4, img: "/Cards/OP08/OP08-023.png", role: "Character" },
	{ code: "OP14-029", name: "Tashigi", count: 4, img: "/Cards/OP14/OP14-029.jpg", role: "Character" },
	{ code: "OP14-033", name: "Perona", count: 4, img: "/Cards/OP14/OP14-033.jpg", role: "Character" },
	{ code: "OP14-027", name: "Shanks", count: 4, img: "/Cards/OP14/OP14-027.jpg", role: "Character" },
	{ code: "OP14-119", name: "Dracule Mihawk", count: 3, img: "/Cards/OP14/OP14-119.jpg", role: "Character" },
	{ code: "ST24-004", name: "Law & Bepo", count: 4, img: "/Cards/ST24/ST24-004.jpg", role: "Character" },
	{ code: "OP06-038", name: "The Billion-fold World Trichiliocosm", count: 4, img: "/Cards/OP06/OP06-038.png", role: "Event" },
	{ code: "OP12-037", name: "Demon Aura Nine Sword Style Asura Blades Drawn Dead Man's Game", count: 3, img: "/Cards/OP12/OP12-037.jpg", role: "Event" },
	{ code: "OP13-040", name: "I Know You're Strong... So I'll Go All Out from the Very Start!!!", count: 2, img: "/Cards/OP13/OP13-040.jpg", role: "Event" },
	{ code: "OP04-035", name: "Spiderweb", count: 2, img: "/Cards/OP04/OP04-035.png", role: "Event" },
	{ code: "OP14-039", name: "Coffin Boat", count: 4, img: "/Cards/OP14/OP14-039.jpg", role: "Stage" },
];

const optionsGroups = [
	{
		cards: [
			{ code: "OP01-041", name: "Kouzuki Momonosuke", img: "/Cards/OP01/OP01-041.png" },
			{ code: "OP07-022", name: "Otama", img: "/Cards/OP07/OP07-022.png" },
			{ code: "OP01-055", name: "You Can Be My Samurai!!", img: "/Cards/OP01/OP01-055.png" },
		],
		note:
			"Wano pack is small but powerful for draw. You need play them in this rateo for maximize their effect but is a little inconsistent.",
	},
	{
		cards: [
			{ code: "ST02-007", name: "Jewelry Bonney", img: "/Cards/ST02/ST02-007.png" },
			{ code: "ST24-002", name: "Kid & Killer", img: "/Cards/ST24/ST24-002.jpg" },
			{ code: "OP12-118", name: "Jewelry Bonney", img: "/Cards/OP12/OP12-118.jpg" },
			{ code: "OP13-031", name: "Trafalgar Law", img: "/Cards/OP13/OP13-031.jpg" },
		],
		note:
			"Supernova pack is very strong, more consistent that Wano pack and open the deck to various option like Law or triple searcher. Not to be underestimated.",
	},
];

const mulliganCards = [
	{ code: "OP12-034", name: "Perona", img: "/Cards/OP12/OP12-034.jpg" },
	{ code: "OP14-039", name: "Coffin Boat", img: "/Cards/OP14/OP14-039.jpg" },
	{ code: "OP14-033", name: "Perona", img: "/Cards/OP14/OP14-033.jpg" },
	{ code: "OP14-029", name: "Tashigi", img: "/Cards/OP14/OP14-029.jpg" },
	{ code: "OP08-023", name: "Carrot", img: "/Cards/OP08/OP08-023.png" },
];

const curve = [
	{
		label: "1/2 DON",
		notes: "Perona for missing Character. Stage for value the Leader ability.",
		cards: [
			{ code: "OP12-034", name: "Perona", img: "/Cards/OP12/OP12-034.jpg" },
			{ code: "OP14-039", name: "Coffin Boat", img: "/Cards/OP14/OP14-039.jpg" },
		],
	},
	{
		label: "3/4 DON",
		notes: "This turn is usually “empty”. Take advantage of it to develop your board or for refresh your hand.",
		cards: [
			{ code: "OP12-034", name: "Perona", img: "/Cards/OP12/OP12-034.jpg" },
			{ code: "OP14-023", name: "Kikunojo", img: "/Cards/OP14/OP14-023.jpg" },
			{ code: "OP14-039", name: "Coffin Boat", img: "/Cards/OP14/OP14-039.jpg" },
		],
	},
	{
		label: "5/6 DON",
		notes: "Perona is good for stall this turn. Tashigi is your play against removal deck. Carrot have value versus Boa for continued to freeze.",
		cards: [
			{ code: "OP14-033", name: "Perona", img: "/Cards/OP14/OP14-033.jpg" },
			{ code: "OP14-029", name: "Tashigi", img: "/Cards/OP14/OP14-029.jpg" },
			{ code: "OP08-023", name: "Carrot", img: "/Cards/OP08/OP08-023.png" },
		],
	},
	{
		label: "7/8 DON",
		notes: "-1K on opponent board. You can rest it for Leader ability, nothing to say.",
		cards: [
			{ code: "OP14-027", name: "Shanks", img: "/Cards/OP14/OP14-027.jpg" },
		],
	},
	{
		label: "9/10 DON",
		notes: "Late game is very strong especially if you chain this two guys. Leader can rest Mihawk for activate his effect in the play turn, joke your opponent and rest Law/Bepo if you don’t have much counter. 11K Character is more that 7K leader. In any case remember to stay with DON open for counter spell.",
		cards: [
			{ code: "OP14-119", name: "Dracule Mihawk", img: "/Cards/OP14/OP14-119.jpg" },
			{ code: "ST24-004", name: "Law & Bepo", img: "/Cards/ST24/ST24-004.jpg" },
		],
	},
];

const tips = [
	{
		title:
			"Play Tashigi and use Leader ability on Stage. Restand 3 DON and use Tashigi effect on 2 DON (if you don’t have a searcher to rest). End of turn restand one DON for Stage, this help Character from removal.",
		cards: [
			{ code: "OP14-020", name: "Dracule Mihawk", img: "/Cards/OP14/OP14-020.jpg" },
			{ code: "OP14-039", name: "Coffin Boat", img: "/Cards/OP14/OP14-039.jpg" },
			{ code: "OP14-029", name: "Tashigi", img: "/Cards/OP14/OP14-029.jpg" },
		],
	},
	{
		title:
			"Remember that you can use Leader ability on this Characters and win in the same turn that you play them.",
		cards: [
			{ code: "OP14-020", name: "Dracule Mihawk", img: "/Cards/OP14/OP14-020.jpg" },
			{ code: "OP14-027", name: "Shanks", img: "/Cards/OP14/OP14-027.jpg" },
			{ code: "OP14-119", name: "Dracule Mihawk", img: "/Cards/OP14/OP14-119.jpg" },
		],
	},
	{
		title:
			"Use Asura main effect and rest 2 opponent Characters, next step: play Carrot if you rest one DON + one Character or use other spell main effect for freeze two opponent 7C.",
		cards: [
			{ code: "OP06-038", name: "The Billion-fold World Trichiliocosm", img: "/Cards/OP06/OP06-038.png" },
			{ code: "OP08-023", name: "Carrot", img: "/Cards/OP08/OP08-023.png" },
			{ code: "OP13-040", name: "I Know You're Strong... So I'll Go All Out from the Very Start!!!", img: "/Cards/OP13/OP13-040.jpg" },
		],
	},
];

const matchups = [
	{
		label: "Even/Slight Disadv.",
		icon: "↓",
		leader: { code: "OP14-041", name: "Boa Hancock", img: "/Cards/OP14/OP14-041.jpg" },
		notes: ["Start 1st", "Manage your life", "Carrot/Shanks key cards"],
	},
	{
		label: "Positive MU",
		icon: "↑",
		leader: { code: "OP13-079", name: "Imu", img: "/Cards/OP13/OP13-079.jpg" },
		notes: ["Start 2nd", "Manage your board", "Push"],
	},
	{
		label: "Even",
		icon: "≈",
		leader: { code: "OP14-020", name: "Dracule Mihawk", img: "/Cards/OP14/OP14-020.jpg" },
		notes: ["Start 1st", "Board control", "Law/Bepo key card"],
	},
	{
		label: "Even/Slight Disadv.",
		icon: "↓",
		leader: { code: "OP14-060", name: "Donquixote Doflamingo", img: "/Cards/OP14/OP14-060.jpg" },
		notes: ["Start 1st", "Manage your board"],
	},
	{
		label: "Positive MU",
		icon: "↑",
		leader: { code: "OP13-002", name: "Ace", img: "/Cards/OP13/OP13-002.jpg" },
		notes: ["Start 1st", "Play on curve", "Asura key card"],
	},
	{
		label: "Positive MU",
		icon: "↑",
		leader: { code: "OP11-040", name: "Monkey.D.Luffy", img: "/Cards/OP11/OP11-040.jpg" },
		notes: ["Start 1st", "Play on curve", "Asura key card"],
	},
];
---

<MainLayout title={title} description={description}>
	<section class="hero guide-hero">
		<div class="container hero-grid">
			<div>
				<span class="hero-badge">March Guide</span>
				<h1>Dracule Mihawk OP14 Deck Guide</h1>
				<p>
					Complete reference guide for Dracule Mihawk OP14: decklist, curve checkpoints, mulligan priorities,
					tech options, and matchup focus leaders. Built for fast reading between rounds.
				</p>
				<div class="hero-actions">
					<a class="btn btn-primary" href="#decklist">Jump to Decklist</a>
					<a class="btn btn-secondary" href="#matchups">Matchups</a>
				</div>
			</div>
			<div class="guide-icon">
				<img src={icon} alt="Dracule Mihawk OP14 guide icon" loading="lazy" />
			</div>
		</div>
	</section>

	<section id="decklist" class="list-section">
		<div class="container">
			<div class="section-header">
				<div>
					<h2>Decklist</h2>
					<p>Main list for Dracule Mihawk OP14.</p>
				</div>
			</div>
			<div class="deck-grid">
				{decklist.map((card) => (
					<button
						class="deck-card"
						type="button"
						data-card-preview
						data-full={card.img}
						data-name={`${card.code} ${card.name}`}
					>
						<img loading="lazy" src={card.img} alt={`${card.code} ${card.name}`} />
						<div class="deck-info">
							<span class="deck-count">{card.count}x</span>
							<div>
								<div class="deck-name">{card.name}</div>
								<div class="deck-code">{card.code}</div>
							</div>
						</div>
						<span class="deck-role">{card.role}</span>
					</button>
				))}
			</div>
		</div>
	</section>

	<section id="options" class="list-section">
		<div class="container">
			<div class="section-header">
				<div>
					<h2>Options & Tech Cards</h2>
					<p>Alternative packs and insert options.</p>
				</div>
			</div>
			<div class="options-stack">
				{optionsGroups.map((group) => (
					<div class="option-group">
						<div class="options-grid">
							{group.cards.map((card) => (
								<button
									class="option-card"
									type="button"
									data-card-preview
									data-full={card.img}
									data-name={`${card.code} ${card.name}`}
								>
									<img loading="lazy" src={card.img} alt={`${card.code} ${card.name}`} />
									<div class="option-info">
										<div class="deck-name">{card.name}</div>
										<div class="deck-code">{card.code}</div>
									</div>
								</button>
							))}
						</div>
						<p class="section-note">{group.note}</p>
					</div>
				))}
			</div>
		</div>
	</section>

	<section id="mulligan" class="list-section">
		<div class="container">
			<div class="section-header">
				<div>
					<h2>Mulligan</h2>
					<p>1st or 2nd this deck need to see 1C search for Leader effect plus 5C for play at the best your curve (you can’t miss the curve).</p>
				</div>
			</div>
			<div class="mulligan-grid">
				{mulliganCards.map((card) => (
					<button
						class="mini-card"
						type="button"
						data-card-preview
						data-full={card.img}
						data-name={`${card.code} ${card.name}`}
					>
						<img loading="lazy" src={card.img} alt={`${card.code} ${card.name}`} />
						<div class="mini-label">{card.name}</div>
					</button>
				))}
			</div>
			<p class="mulligan-text">If you don’t see this combo mulligan is the way.</p>
		</div>
	</section>

	<section id="curve" class="list-section">
		<div class="container">
			<div class="section-header">
				<div>
					<h2>Curve</h2>
					<p>Turn checkpoints and key curve reminders.</p>
				</div>
			</div>
			<div class="curve-block">
				<h3>Curve</h3>
				<div class="curve-rows">
					{curve.map((slot) => (
						<div class="curve-row">
							<div class="curve-label">{slot.label}</div>
							<div class="curve-cards">
								{slot.cards.map((card) => (
									<button
										class="mini-card"
										type="button"
										data-card-preview
										data-full={card.img}
										data-name={`${card.code} ${card.name}`}
									>
										<img loading="lazy" src={card.img} alt={`${card.code} ${card.name}`} />
									</button>
								))}
							</div>
							<div class="curve-note">{slot.notes}</div>
						</div>
					))}
				</div>
			</div>
		</div>
	</section>

	<section id="tips" class="list-section">
		<div class="container">
			<div class="section-header">
				<div>
					<h2>Tips</h2>
					<p>Sequencing reminders and key card interactions.</p>
				</div>
			</div>
			<div class="tips-grid">
				{tips.map((tip) => (
					<div class="tip-card">
						<div class="tip-cards">
							{tip.cards.map((card) => (
								<button
									class="mini-card"
									type="button"
									data-card-preview
									data-full={card.img}
									data-name={`${card.code} ${card.name}`}
								>
									<img loading="lazy" src={card.img} alt={`${card.code} ${card.name}`} />
								</button>
							))}
						</div>
						<p>{tip.title}</p>
					</div>
				))}
			</div>
		</div>
	</section>

	<section id="matchups" class="list-section">
		<div class="container">
			<div class="section-header">
				<div>
					<h2>Matchups</h2>
					<p>Key leaders for the matchup spread.</p>
				</div>
			</div>
			<div class="matchups-grid">
				{matchups.map((matchup) => (
					<div class="matchup-card">
						<div class={`matchup-badge ${matchup.label === "Negative MU" || matchup.label === "Slight Disadv." || matchup.label === "Even/Slight Disadv." ? "badge-down" : matchup.label === "Even" ? "badge-even" : "badge-up"}`}>
							<span class="badge-icon">{matchup.icon}</span>
							<span>{matchup.label}</span>
						</div>
						<button
							class="mini-card"
							type="button"
							data-card-preview
							data-full={matchup.leader.img}
							data-name={`${matchup.leader.code} ${matchup.leader.name}`}
						>
							<img loading="lazy" src={matchup.leader.img} alt={`${matchup.leader.code} ${matchup.leader.name}`} />
						</button>
						<div class="matchup-name">{matchup.leader.name}</div>
						<ul>
							{matchup.notes.map((note) => (
								<li>{note}</li>
							))}
						</ul>
					</div>
				))}
			</div>
		</div>
	</section>

	<div class="card-modal" data-modal aria-hidden="true">
		<div class="card-modal-inner" role="dialog" aria-modal="true" aria-label="Card preview">
			<button class="modal-close" type="button" data-close>Close</button>
			<img src="" alt="" data-modal-image />
		</div>
	</div>
</MainLayout>

<script>
	const previewButtons = document.querySelectorAll("[data-card-preview]");
	const modal = document.querySelector("[data-modal]");
	const modalImage = document.querySelector("[data-modal-image]");
	const closeButton = document.querySelector("[data-close]");

	const openModal = (button) => {
		if (!modal || !modalImage) return;
		const full = button.getAttribute("data-full");
		const name = button.getAttribute("data-name") || "Card preview";
		modalImage.setAttribute("src", full);
		modalImage.setAttribute("alt", name);
		modal.classList.add("is-open");
		modal.setAttribute("aria-hidden", "false");
	};

	const closeModal = () => {
		if (!modal) return;
		modal.classList.remove("is-open");
		modal.setAttribute("aria-hidden", "true");
	};

	previewButtons.forEach((button) => {
		button.addEventListener("click", () => openModal(button));
	});

	closeButton?.addEventListener("click", closeModal);
	modal?.addEventListener("click", (event) => {
		if (event.target === modal) closeModal();
	});
	window.addEventListener("keydown", (event) => {
		if (event.key === "Escape") closeModal();
	});
</script>

<style>
	.guide-hero .hero-grid {
		grid-template-columns: minmax(0, 1.1fr) minmax(0, 0.9fr);
	}

	.guide-icon {
		display: grid;
		place-items: center;
	}

	.guide-icon img {
		width: min(320px, 90%);
		border-radius: 18px;
		border: 1px solid rgba(255, 255, 255, 0.08);
		box-shadow: var(--shadow);
	}

	.deck-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
		gap: 1.2rem;
	}

	.deck-card {
		background: rgba(12, 17, 30, 0.9);
		border: 1px solid var(--border);
		border-radius: var(--radius-md);
		padding: 0.9rem;
		display: grid;
		gap: 0.75rem;
		text-align: left;
		cursor: pointer;
		transition: transform 0.2s ease, border 0.2s ease, box-shadow 0.2s ease;
	}

	.deck-card:hover,
	.deck-card:focus-visible {
		transform: translateY(-4px);
		border-color: rgba(255, 107, 61, 0.4);
		box-shadow: var(--shadow);
		outline: none;
	}

	.deck-card img {
		width: 100%;
		max-width: 150px;
		margin: 0 auto;
		border-radius: 12px;
	}

	.deck-info {
		display: grid;
		grid-template-columns: auto 1fr;
		gap: 0.6rem;
		align-items: center;
	}

	.deck-count {
		background: rgba(255, 107, 61, 0.15);
		color: var(--accent);
		font-weight: 700;
		border-radius: 999px;
		padding: 0.3rem 0.7rem;
		font-size: 0.9rem;
	}

	.deck-name {
		font-weight: 600;
	}

	.deck-code {
		color: var(--text-300);
		font-size: 0.85rem;
	}

	.deck-role {
		color: var(--text-300);
		font-size: 0.85rem;
		text-transform: uppercase;
		letter-spacing: 1px;
	}

	.options-grid {
		display: flex;
		flex-wrap: wrap;
		justify-content: center;
		align-items: flex-start;
		gap: 1rem;
	}

	.options-stack {
		display: grid;
		gap: 1.2rem;
	}

	.option-group {
		display: grid;
		gap: 1rem;
		padding: 1rem;
		border: 1px solid var(--border);
		border-radius: var(--radius-md);
		background: rgba(12, 17, 30, 0.55);
	}

	.option-card {
		background: rgba(12, 17, 30, 0.9);
		border: 1px solid var(--border);
		border-radius: var(--radius-md);
		padding: 0.8rem;
		display: grid;
		gap: 0.6rem;
		width: min(220px, 100%);
		justify-items: center;
		text-align: center;
		cursor: pointer;
		transition: transform 0.2s ease, border 0.2s ease, box-shadow 0.2s ease;
	}

	.option-card:hover,
	.option-card:focus-visible {
		transform: translateY(-4px);
		border-color: rgba(255, 107, 61, 0.4);
		box-shadow: var(--shadow);
		outline: none;
	}

	.option-card img {
		width: 100%;
		max-width: 220px;
		margin: 0 auto;
		border-radius: 10px;
	}

	.option-info {
		display: grid;
		gap: 0.2rem;
		text-align: center;
	}

	.option-note {
		margin: 0.4rem 0 0;
		color: var(--text-300);
		font-size: 0.9rem;
	}

	.note-block {
		background: rgba(14, 18, 30, 0.7);
		border: 1px solid var(--border);
		border-radius: var(--radius-md);
		padding: 1.2rem 1.4rem;
	}

	.mulligan-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
		gap: 1rem;
		margin-bottom: 1rem;
	}

	.mulligan-text {
		color: var(--text-200);
	}

	.curve-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
		gap: 1.2rem;
	}

	.curve-block {
		background: rgba(12, 17, 30, 0.9);
		border: 1px solid var(--border);
		border-radius: var(--radius-md);
		padding: 1.2rem;
		margin-bottom: 1.2rem;
	}

	.curve-rows {
		display: grid;
		gap: 1rem;
		margin-top: 0.8rem;
	}

	.curve-row {
		display: grid;
		grid-template-columns: 80px minmax(0, 1fr) minmax(0, 1.2fr);
		gap: 1rem;
		align-items: center;
	}

	.curve-label {
		font-weight: 700;
		color: var(--accent);
	}

	.curve-cards {
		display: flex;
		flex-wrap: wrap;
		gap: 0.6rem;
	}

	.curve-note {
		color: var(--text-300);
	}

	.mini-card {
		border: none;
		background: transparent;
		cursor: pointer;
		padding: 0;
		border-radius: 10px;
	}

	.mini-card img {
		width: 120px;
		border-radius: 10px;
		display: block;
		box-shadow: 0 10px 20px rgba(0, 0, 0, 0.35);
	}

	.mini-label {
		margin-top: 0.4rem;
		font-size: 0.85rem;
		color: var(--text-300);
		text-align: center;
	}

	.tips-grid {
		display: grid;
		gap: 1.2rem;
	}

	.tip-card {
		background: rgba(12, 17, 30, 0.9);
		border: 1px solid var(--border);
		border-radius: var(--radius-md);
		padding: 1rem 1.2rem;
		display: grid;
		gap: 0.8rem;
	}

	.tip-cards {
		display: flex;
		gap: 0.8rem;
		flex-wrap: wrap;
	}

	.matchups-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
		gap: 1rem;
	}

	.matchup-card {
		background: rgba(12, 17, 30, 0.9);
		border: 1px solid var(--border);
		border-radius: var(--radius-md);
		padding: 1rem;
		display: grid;
		gap: 0.6rem;
	}

	.matchup-badge {
		font-size: 0.8rem;
		text-transform: uppercase;
		letter-spacing: 1px;
		font-weight: 700;
		display: inline-flex;
		align-items: center;
		gap: 0.45rem;
		padding: 0.25rem 0.6rem;
		border-radius: 999px;
		border: 1px solid rgba(255, 255, 255, 0.08);
		background: rgba(12, 17, 30, 0.8);
	}

	.badge-icon {
		width: 26px;
		height: 26px;
		border-radius: 999px;
		display: grid;
		place-items: center;
		font-size: 1.1rem;
		font-weight: 800;
		color: #0b0f1a;
	}

	.badge-up .badge-icon {
		background: linear-gradient(135deg, #22c55e, #86efac);
	}

	.badge-down .badge-icon {
		background: linear-gradient(135deg, #ef4444, #fca5a5);
	}

	.badge-even .badge-icon {
		background: linear-gradient(135deg, #f59e0b, #fbbf24);
	}

	.matchup-name {
		font-weight: 600;
	}

	.curve-card {
		background: rgba(12, 17, 30, 0.9);
		border: 1px solid var(--border);
		border-radius: var(--radius-md);
		padding: 1.2rem;
	}

	.section-note {
		color: var(--text-300);
		max-width: 68ch;
		margin: 0 auto;
		text-align: center;
		line-height: 1.65;
		font-size: clamp(0.95rem, 0.88rem + 0.18vw, 1rem);
	}

	.card-modal {
		position: fixed;
		inset: 0;
		background: rgba(6, 8, 14, 0.85);
		display: none;
		align-items: center;
		justify-content: center;
		padding: 2rem;
		z-index: 40;
	}

	.card-modal.is-open {
		display: flex;
	}

	.card-modal-inner {
		max-width: min(520px, 90vw);
		max-height: 90vh;
		background: rgba(10, 14, 24, 0.9);
		border: 1px solid var(--border);
		border-radius: var(--radius-lg);
		padding: 1rem 1rem 1.5rem;
		position: relative;
		box-shadow: var(--shadow);
	}

	.card-modal-inner img {
		width: 100%;
		max-height: 80vh;
		object-fit: contain;
	}

	.modal-close {
		position: absolute;
		top: 0.75rem;
		right: 0.75rem;
		background: rgba(255, 255, 255, 0.05);
		border: 1px solid var(--border);
		color: var(--text-100);
		border-radius: 999px;
		padding: 0.35rem 0.8rem;
		cursor: pointer;
		font-size: 0.85rem;
	}

	@media (max-width: 900px) {
		.guide-hero .hero-grid {
			grid-template-columns: 1fr;
		}
	}

	@media (max-width: 640px) {
		.option-group {
			padding: 0.85rem;
		}

		.deck-card img {
			max-width: 120px;
		}

		.option-card img {
			max-width: 170px;
		}

		.mini-card img {
			width: 90px;
		}
	}
</style>

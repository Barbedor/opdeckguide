---
import MainLayout from "../../layouts/MainLayout.astro";

const title = "Sanji OP12 Deck Guide";
const description =
	"Sanji OP12 deck guide for One Piece Card Game with decklist, mulligan priorities, curve notes, tech cards, and matchup plans.";

const icon = "/assets/guides/sanji-op12.png";

const decklist = [
	{ code: "OP12-041", name: "Sanji", count: 1, img: "/Cards/OP12/OP12-041.jpg", role: "Leader" },
	{ code: "OP15-047", name: "Sanji", count: 2, img: "/Cards/OP15/OP15-047.jpg", role: "Character" },
	{ code: "OP07-051", name: "Boa Hancock", count: 2, img: "/Cards/OP07/OP07-051.png", role: "Character" },
	{ code: "OP12-070", name: "Sanji", count: 4, img: "/Cards/OP12/OP12-070.jpg", role: "Character" },
	{ code: "OP12-063", name: "Reiju", count: 3, img: "/Cards/OP12/OP12-063.jpg", role: "Character" },
	{ code: "OP07-064", name: "Sanji", count: 4, img: "/Cards/OP07/OP07-064.png", role: "Character" },
	{ code: "EB04-029", name: "Lady's Teardrops", count: 4, img: "/Cards/EB04/EB04-029.jpg", role: "Event" },
	{ code: "OP11-060", name: "Let's Crash This Wedding", count: 3, img: "/Cards/OP11/OP11-060.jpg", role: "Event" },
	{ code: "OP12-059", name: "Concasser", count: 2, img: "/Cards/OP12/OP12-059.jpg", role: "Event" },
	{ code: "OP11-061", name: "Gum Gum Culverin", count: 2, img: "/Cards/OP11/OP11-061.jpg", role: "Event" },
	{ code: "OP12-060", name: "Boeuf Burst", count: 4, img: "/Cards/OP12/OP12-060.jpg", role: "Event" },
	{ code: "OP13-076", name: "Divine Departure", count: 2, img: "/Cards/OP13/OP13-076.jpg", role: "Event" },
	{ code: "OP15-078", name: "Mamaragan", count: 4, img: "/Cards/OP15/OP15-078.jpg", role: "Event" },
	{ code: "OP09-078", name: "Gum Gum Giant", count: 4, img: "/Cards/OP09/OP09-078.jpg", role: "Event" },
	{ code: "OP12-079", name: "Luffy Is The Man...", count: 4, img: "/Cards/OP12/OP12-079.jpg", role: "Event" },
	{ code: "EB04-041", name: "Stealth Black", count: 4, img: "/Cards/EB04/EB04-041.jpg", role: "Event" },
	{ code: "OP12-078", name: "Brochette Blow", count: 2, img: "/Cards/OP12/OP12-078.jpg", role: "Event" },
];

const options = [
	{
		code: "EB03-031",
		name: "Reiju Vinsmoke",
		img: "/Cards/EB03/EB03-031.jpg",
		note: "Lets you replay Gravity Blade lines and gives access to event turns again.",
	},
	{
		code: "EB01-039",
		name: "Ragnarok",
		img: "/Cards/EB01/EB01-039.png",
		note: "Useful for answering larger 7-cost bodies such as Sabo or EB04 Zoro.",
	},
	{
		code: "P-055",
		name: "Luffy",
		img: "/Cards/P/P-055.png",
		note: "Can force almost any body off the board, which matters in some big-body situations.",
	},
	{
		code: "OP10-060",
		name: "Bari Bari Pistol",
		img: "/Cards/OP10/OP10-060.jpg",
		note: "Strong into larger bodies and also works well with Brochette Blow or Reiju.",
	},
	{
		code: "OP12-071",
		name: "Charlotte Pudding",
		img: "/Cards/OP12/OP12-071.jpg",
		note: "Finds Sanji or an event and can also clean up 2000-power or lower bodies.",
	},
	{
		code: "OP07-076",
		name: "Noro Noro Beam Sword",
		img: "/Cards/OP07/OP07-076.png",
		note: "A defensive counter that can also rest a body regardless of cost.",
	},
];

const mulliganCards = [
	{ code: "OP12-070", name: "Sanji", img: "/Cards/OP12/OP12-070.jpg" },
	{ code: "EB04-029", name: "Lady's Teardrops", img: "/Cards/EB04/EB04-029.jpg" },
	{ code: "OP11-060", name: "Let's Crash This Wedding", img: "/Cards/OP11/OP11-060.jpg" },
	{ code: "OP12-060", name: "Boeuf Burst", img: "/Cards/OP12/OP12-060.jpg" },
];

const curve = [
	{
		label: "1/2 DON!!",
		cards: [
			{ code: "OP11-060", name: "Let's Crash This Wedding", img: "/Cards/OP11/OP11-060.jpg" },
			{ code: "EB04-029", name: "Lady's Teardrops", img: "/Cards/EB04/EB04-029.jpg" },
			{ code: "OP12-079", name: "Luffy Is The Man...", img: "/Cards/OP12/OP12-079.jpg" },
		],
		notes:
			"These opening turns are mainly for finding OP12-070 Sanji and getting the setup moving without wasting pieces.",
	},
	{
		label: "3/4 DON!!",
		cards: [
			{ code: "OP12-070", name: "Sanji", img: "/Cards/OP12/OP12-070.jpg" },
			{ code: "OP12-060", name: "Boeuf Burst", img: "/Cards/OP12/OP12-060.jpg" },
			{ code: "OP07-064", name: "Sanji", img: "/Cards/OP07/OP07-064.png" },
			{ code: "EB04-041", name: "Stealth Black", img: "/Cards/EB04/EB04-041.jpg" },
		],
		notes:
			"Sanji OP12-070 with leader effect into Boeuf Burst is the clean line here, while Lady's Teardrops can keep digging for OP12-070 Sanji, blocker Sanji, or Stealth Black.",
	},
	{
		label: "5/6 DON!!",
		cards: [
			{ code: "OP12-063", name: "Reiju", img: "/Cards/OP12/OP12-063.jpg" },
			{ code: "OP12-070", name: "Sanji", img: "/Cards/OP12/OP12-070.jpg" },
			{ code: "EB04-041", name: "Stealth Black", img: "/Cards/EB04/EB04-041.jpg" },
		],
		notes:
			"Reiju or OP12-070 Sanji starts the stronger midgame turns, then Stealth Black can bring another Sanji while still leaving room for a 1-cost event.",
	},
	{
		label: "7/8 DON!!",
		cards: [
			{ code: "OP07-051", name: "Boa Hancock", img: "/Cards/OP07/OP07-051.png" },
			{ code: "EB04-041", name: "Stealth Black", img: "/Cards/EB04/EB04-041.jpg" },
			{ code: "OP12-060", name: "Boeuf Burst", img: "/Cards/OP12/OP12-060.jpg" },
			{ code: "OP11-061", name: "Gum Gum Culverin", img: "/Cards/OP11/OP11-061.jpg" },
		],
		notes:
			"If Hancock is available, this is a strong point to deploy her, then convert the turn with Stealth Black or Boeuf Burst. Culverin is the board-control tool when the turn needs removal more than pressure.",
	},
	{
		label: "Late Game",
		cards: [
			{ code: "OP12-078", name: "Brochette Blow", img: "/Cards/OP12/OP12-078.jpg" },
			{ code: "OP13-076", name: "Divine Departure", img: "/Cards/OP13/OP13-076.jpg" },
			{ code: "OP15-047", name: "Sanji", img: "/Cards/OP15/OP15-047.jpg" },
			{ code: "OP09-078", name: "Gum Gum Giant", img: "/Cards/OP09/OP09-078.jpg" },
			{ code: "OP15-078", name: "Mamaragan", img: "/Cards/OP15/OP15-078.jpg" },
		],
		notes:
			"Brochette Blow and Divine Departure are the main answers to larger bodies. Sanji OP15-047 helps force through bigger blockers and is a good Stealth Black re-summon target. Gum Gum Giant keeps you safe while drawing, and Mamaragan clears blockers at 5000 power or less.",
	},
];

const matchups = [
	{
		label: "Go 1st",
		icon: "1",
		leader: { code: "OP13-079", name: "Sabo", img: "/Cards/OP13/OP13-079.jpg" },
		notes: [
			"Use Culverin and Boeuf Burst to keep the board under control.",
			"Stealth Black plus Sanji OP15 is the main finishing line.",
		],
	},
	{
		label: "Go 2nd",
		icon: "2",
		leader: { code: "OP13-002", name: "Portgas.D.Ace", img: "/Cards/OP13/OP13-002.jpg" },
		notes: [
			"Stealth Black plus Sanji OP15 is also the main closing combo here.",
			"Divine Departure matters for cards like Zoro.",
		],
	},
	{
		label: "Go 1st",
		icon: "1",
		leader: { code: "OP11-041", name: "Nami", img: "/Cards/OP11/OP11-041.jpg" },
		notes: [
			"Gravity Blade lines matter against Nami, Borsalino, or OP15 Zoro.",
			"Gum Gum Giant helps you survive longer while the game stretches.",
		],
	},
];
---

<MainLayout title={title} description={description}>
	<section class="hero guide-hero">
		<div class="container hero-grid">
			<div>
				<span class="hero-badge">Deck Guide</span>
				<h1>Sanji OP12 Deck Guide</h1>
				<p>
					Reference guide for Sanji OP12 with the full list, mulligan priorities, curve checkpoints, event-heavy
					midgame lines, and quick matchup plans. Built to stay easy to scan between rounds.
				</p>
				<div class="hero-actions">
					<a class="btn btn-primary" href="#decklist">Jump to Decklist</a>
					<a class="btn btn-secondary" href="#matchups">Matchups</a>
				</div>
			</div>
			<div class="guide-icon">
				<img src={icon} alt="Sanji OP12 guide icon" loading="lazy" />
			</div>
		</div>
	</section>

	<section id="decklist" class="list-section">
		<div class="container">
			<div class="section-header">
				<div>
					<h2>Decklist</h2>
					<p>Main list for Sanji OP12.</p>
				</div>
			</div>
			<div class="deck-grid">
				{decklist.map((card) => (
					<button
						class="deck-card"
						type="button"
						data-card-preview
						data-full={card.img}
						data-name={`${card.code} ${card.name}`}
					>
						<img loading="lazy" src={card.img} alt={`${card.code} ${card.name}`} />
						<div class="deck-info">
							<span class="deck-count">{card.count}x</span>
							<div>
								<div class="deck-name">{card.name}</div>
								<div class="deck-code">{card.code}</div>
							</div>
						</div>
						<span class="deck-role">{card.role}</span>
					</button>
				))}
			</div>
		</div>
	</section>

	<section id="options" class="list-section">
		<div class="container">
			<div class="section-header">
				<div>
					<h2>Options & Tech Cards</h2>
					<p>Flexible choices that support specific board states or defensive turns.</p>
				</div>
			</div>
			<div class="options-grid options-grid-detailed">
				{options.map((card) => (
					<div class="option-item">
						<button
							class="option-card"
							type="button"
							data-card-preview
							data-full={card.img}
							data-name={`${card.code} ${card.name}`}
						>
							<img loading="lazy" src={card.img} alt={`${card.code} ${card.name}`} />
							<div class="option-info">
								<div class="deck-name">{card.name}</div>
								<div class="deck-code">{card.code}</div>
							</div>
						</button>
						<p class="option-note">{card.note}</p>
					</div>
				))}
			</div>
		</div>
	</section>

	<section id="mulligan" class="list-section">
		<div class="container">
			<div class="section-header">
				<div>
					<h2>Mulligan</h2>
					<p>Prioritize the pieces that find Sanji quickly and keep the trash and event flow online.</p>
				</div>
			</div>
			<div class="mulligan-grid">
				{mulliganCards.map((card) => (
					<button
						class="mini-card"
						type="button"
						data-card-preview
						data-full={card.img}
						data-name={`${card.code} ${card.name}`}
					>
						<img loading="lazy" src={card.img} alt={`${card.code} ${card.name}`} />
						<div class="mini-label">{card.name}</div>
					</button>
				))}
			</div>
			<p class="mulligan-text">
				OP12-070 Sanji plus Lady&apos;s Teardrops is the clean start: it finds Sanji while also filling the trash.
				Let&apos;s Crash This Wedding is another strong opener because it looks for OP12-070 Sanji, and Boeuf Burst
				is the event you most want to have ready for the midgame or for the draw 2 line.
			</p>
		</div>
	</section>

	<section id="curve" class="list-section">
		<div class="container">
			<div class="section-header">
				<div>
					<h2>Curve Guide</h2>
					<p>Turn checkpoints for the search package, Sanji setup turns, and your late-game event finishers.</p>
				</div>
			</div>
			<div class="curve-block">
				<h3>Curve</h3>
				<div class="curve-rows">
					{curve.map((slot) => (
						<div class="curve-row">
							<div class="curve-label">{slot.label}</div>
							<div class="curve-cards">
								{slot.cards.map((card) => (
									<button
										class="mini-card"
										type="button"
										data-card-preview
										data-full={card.img}
										data-name={`${card.code} ${card.name}`}
									>
										<img loading="lazy" src={card.img} alt={`${card.code} ${card.name}`} />
									</button>
								))}
							</div>
							<div class="curve-note">{slot.notes}</div>
						</div>
					))}
				</div>
			</div>
		</div>
	</section>

	<section id="tips" class="list-section">
		<div class="container">
			<div class="section-header">
				<div>
					<h2>Tips</h2>
				</div>
			</div>
			<div class="tips-grid tips-grid-empty"></div>
		</div>
	</section>

	<section id="matchups" class="list-section">
		<div class="container">
			<div class="section-header">
				<div>
					<h2>Matchups</h2>
					<p>Quick turn-order reminders and the main cards that matter in the pairing.</p>
				</div>
			</div>
			<div class="matchups-grid">
				{matchups.map((matchup) => (
					<div class="matchup-card">
						<div class="matchup-badge badge-plan">
							<span class="badge-icon">{matchup.icon}</span>
							<span>{matchup.label}</span>
						</div>
						<button
							class="mini-card"
							type="button"
							data-card-preview
							data-full={matchup.leader.img}
							data-name={`${matchup.leader.code} ${matchup.leader.name}`}
						>
							<img loading="lazy" src={matchup.leader.img} alt={`${matchup.leader.code} ${matchup.leader.name}`} />
						</button>
						<div class="matchup-name">{matchup.leader.name}</div>
						<div class="deck-code">{matchup.leader.code}</div>
						<ul>
							{matchup.notes.map((note) => (
								<li>{note}</li>
							))}
						</ul>
					</div>
				))}
			</div>
		</div>
	</section>

	<div class="card-modal" data-modal aria-hidden="true">
		<div class="card-modal-inner" role="dialog" aria-modal="true" aria-label="Card preview">
			<button class="modal-close" type="button" data-close>Close</button>
			<img src="" alt="" data-modal-image />
		</div>
	</div>
</MainLayout>

<script>
	const previewButtons = document.querySelectorAll("[data-card-preview]");
	const modal = document.querySelector("[data-modal]");
	const modalImage = document.querySelector("[data-modal-image]");
	const closeButton = document.querySelector("[data-close]");

	const openModal = (button) => {
		if (!modal || !modalImage) return;
		const full = button.getAttribute("data-full");
		const name = button.getAttribute("data-name") || "Card preview";
		modalImage.setAttribute("src", full);
		modalImage.setAttribute("alt", name);
		modal.classList.add("is-open");
		modal.setAttribute("aria-hidden", "false");
	};

	const closeModal = () => {
		if (!modal) return;
		modal.classList.remove("is-open");
		modal.setAttribute("aria-hidden", "true");
	};

	previewButtons.forEach((button) => {
		button.addEventListener("click", () => openModal(button));
	});

	closeButton?.addEventListener("click", closeModal);
	modal?.addEventListener("click", (event) => {
		if (event.target === modal) closeModal();
	});
	window.addEventListener("keydown", (event) => {
		if (event.key === "Escape") closeModal();
	});
</script>

<style>
	.guide-hero .hero-grid {
		grid-template-columns: minmax(0, 1.1fr) minmax(0, 0.9fr);
	}

	.guide-icon {
		display: grid;
		place-items: center;
	}

	.guide-icon img {
		width: min(320px, 90%);
		border-radius: 18px;
		border: 1px solid rgba(255, 255, 255, 0.08);
		box-shadow: var(--shadow);
	}

	.deck-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
		gap: 1.2rem;
	}

	.deck-card {
		background: rgba(12, 17, 30, 0.9);
		border: 1px solid var(--border);
		border-radius: var(--radius-md);
		padding: 0.9rem;
		display: grid;
		gap: 0.75rem;
		text-align: left;
		cursor: pointer;
		transition: transform 0.2s ease, border 0.2s ease, box-shadow 0.2s ease;
	}

	.deck-card:hover,
	.deck-card:focus-visible,
	.option-card:hover,
	.option-card:focus-visible {
		transform: translateY(-4px);
		border-color: rgba(255, 107, 61, 0.4);
		box-shadow: var(--shadow);
		outline: none;
	}

	.deck-card img {
		width: 100%;
		max-width: 150px;
		margin: 0 auto;
		border-radius: 12px;
	}

	.deck-info {
		display: grid;
		grid-template-columns: auto 1fr;
		gap: 0.6rem;
		align-items: center;
	}

	.deck-count {
		background: rgba(255, 107, 61, 0.15);
		color: var(--accent);
		font-weight: 700;
		border-radius: 999px;
		padding: 0.3rem 0.7rem;
		font-size: 0.9rem;
	}

	.deck-name {
		font-weight: 600;
	}

	.deck-code {
		color: var(--text-300);
		font-size: 0.85rem;
	}

	.deck-role {
		color: var(--text-300);
		font-size: 0.85rem;
		text-transform: uppercase;
		letter-spacing: 1px;
	}

	.options-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
		gap: 1rem;
	}

	.option-item {
		display: grid;
		gap: 0.7rem;
	}

	.option-card {
		background: rgba(12, 17, 30, 0.9);
		border: 1px solid var(--border);
		border-radius: var(--radius-md);
		padding: 0.8rem;
		display: grid;
		gap: 0.6rem;
		cursor: pointer;
	}

	.option-card img {
		width: 100%;
		max-width: 220px;
		margin: 0 auto;
		border-radius: 10px;
	}

	.option-info {
		display: grid;
		gap: 0.2rem;
	}

	.option-note,
	.section-note,
	.curve-note {
		color: var(--text-300);
	}

	.mulligan-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
		gap: 1rem;
		margin-bottom: 1rem;
	}

	.mulligan-text {
		color: var(--text-200);
	}

	.curve-block {
		background: rgba(12, 17, 30, 0.9);
		border: 1px solid var(--border);
		border-radius: var(--radius-md);
		padding: 1.2rem;
		margin-bottom: 1.2rem;
	}

	.curve-rows {
		display: grid;
		gap: 1rem;
		margin-top: 0.8rem;
	}

	.curve-row {
		display: grid;
		grid-template-columns: 90px minmax(0, 1fr) minmax(0, 1.2fr);
		gap: 1rem;
		align-items: center;
	}

	.curve-label {
		font-weight: 700;
		color: var(--accent);
	}

	.curve-cards,
	.tip-cards {
		display: flex;
		flex-wrap: wrap;
		gap: 0.6rem;
	}

	.mini-card {
		border: none;
		background: transparent;
		cursor: pointer;
		padding: 0;
		border-radius: 10px;
	}

	.mini-card img {
		width: 120px;
		border-radius: 10px;
		display: block;
		box-shadow: 0 10px 20px rgba(0, 0, 0, 0.35);
	}

	.mini-label {
		margin-top: 0.4rem;
		font-size: 0.85rem;
		color: var(--text-300);
		text-align: center;
	}

	.tips-grid-empty {
		min-height: 8px;
	}

	.matchups-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
		gap: 1rem;
	}

	.matchup-card {
		background: rgba(12, 17, 30, 0.9);
		border: 1px solid var(--border);
		border-radius: var(--radius-md);
		padding: 1rem;
		display: grid;
		gap: 0.55rem;
	}

	.matchup-badge {
		font-size: 0.8rem;
		text-transform: uppercase;
		letter-spacing: 1px;
		font-weight: 700;
		display: inline-flex;
		align-items: center;
		gap: 0.45rem;
		padding: 0.25rem 0.6rem;
		border-radius: 999px;
		border: 1px solid rgba(255, 255, 255, 0.08);
		background: rgba(12, 17, 30, 0.8);
	}

	.badge-icon {
		width: 26px;
		height: 26px;
		border-radius: 999px;
		display: grid;
		place-items: center;
		font-size: 1rem;
		font-weight: 800;
		color: #0b0f1a;
	}

	.badge-plan .badge-icon {
		background: linear-gradient(135deg, #60a5fa, #93c5fd);
	}

	.matchup-name {
		font-weight: 600;
	}

	.card-modal {
		position: fixed;
		inset: 0;
		background: rgba(6, 8, 14, 0.85);
		display: none;
		align-items: center;
		justify-content: center;
		padding: 2rem;
		z-index: 40;
	}

	.card-modal.is-open {
		display: flex;
	}

	.card-modal-inner {
		max-width: min(520px, 90vw);
		max-height: 90vh;
		background: rgba(10, 14, 24, 0.9);
		border: 1px solid var(--border);
		border-radius: var(--radius-lg);
		padding: 1rem 1rem 1.5rem;
		position: relative;
		box-shadow: var(--shadow);
	}

	.card-modal-inner img {
		width: 100%;
		max-height: 80vh;
		object-fit: contain;
	}

	.modal-close {
		position: absolute;
		top: 0.75rem;
		right: 0.75rem;
		background: rgba(255, 255, 255, 0.05);
		border: 1px solid var(--border);
		color: var(--text-100);
		border-radius: 999px;
		padding: 0.35rem 0.8rem;
		cursor: pointer;
		font-size: 0.85rem;
	}

	@media (max-width: 900px) {
		.guide-hero .hero-grid {
			grid-template-columns: 1fr;
		}
	}

	@media (max-width: 640px) {
		.guide-hero .hero-grid {
			grid-template-columns: minmax(0, 1fr) 96px;
			gap: 1rem;
			align-items: start;
		}

		.guide-hero .hero-grid > :first-child {
			min-width: 0;
		}

		.guide-icon {
			grid-column: 2;
			grid-row: 1;
			align-self: start;
			justify-self: end;
		}

		.guide-icon img {
			width: 96px;
			max-width: 96px;
			border-radius: 14px;
		}

		.deck-grid {
			grid-template-columns: repeat(3, minmax(0, 1fr));
			gap: 0.35rem;
		}

		.deck-card {
			padding: 0.35rem;
			gap: 0.4rem;
			border-radius: 12px;
			text-align: center;
		}

		.deck-card img {
			max-width: 94px;
			border-radius: 8px;
		}

		.deck-info {
			grid-template-columns: 1fr;
			justify-items: center;
			gap: 0.28rem;
		}

		.deck-count {
			padding: 0.18rem 0.45rem;
			font-size: 0.68rem;
			line-height: 1;
		}

		.deck-name {
			font-size: 0.68rem;
			line-height: 1.15;
			display: -webkit-box;
			-webkit-line-clamp: 2;
			-webkit-box-orient: vertical;
			overflow: hidden;
		}

		.deck-code {
			font-size: 0.66rem;
			line-height: 1.1;
		}

		.deck-role {
			display: none;
		}

		.option-card img {
			max-width: 180px;
		}

		.mini-card img {
			width: 90px;
		}

		.curve-row {
			display: flex;
			flex-direction: column;
			align-items: stretch;
			gap: 0.55rem;
		}

		.curve-cards {
			display: grid;
			grid-template-columns: repeat(3, minmax(0, 1fr));
			gap: 0.45rem;
			justify-items: start;
			align-items: start;
		}

		.curve-cards .mini-card {
			width: 100%;
		}

		.curve-cards .mini-card img {
			width: 100%;
			max-width: 88px;
		}
	}
</style>
